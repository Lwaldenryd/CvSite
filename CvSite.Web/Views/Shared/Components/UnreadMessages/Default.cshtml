@model int
<span class="d-inline-flex align-items-center">
    Messages
    @if (Model > 0)
    {
        <span class="badge rounded-pill bg-danger ms-1 shadow-sm" style="font-size: 0.7rem; line-height: 1; padding: 0.35em 0.5em;">
            @Model
        </span>
    }
</span>
